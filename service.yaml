apiVersion: v1
kind: Service
metadata:
  name: register-service
spec:
  selector:
    app: register
  type: NodePort
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 30085
